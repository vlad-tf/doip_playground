# Copy to ".env" and fill in your proxy. docker compose auto-loads ".env"
# and passes these as build args (see docker-compose.yml build.args).
# Leave blank to build without a proxy.
HTTP_PROXY=http://YOUR_PROXY:PORT
HTTPS_PROXY=http://YOUR_PROXY:PORT
NO_PROXY=localhost,127.0.0.1,172.30.0.0/16
