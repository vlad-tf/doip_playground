[pytest]
testpaths = tests

# No pytest-asyncio: the suite drives coroutines through the run() helper in
# tests/conftest.py, the same way doip_edgenode/tests/test_middleware.py does.
# `pip3 install --user pytest` is the whole setup.
